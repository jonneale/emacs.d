(ns supplier-data.test.suppliers.eon.acquisition-filter
  (:use [clojure.test]
        [clojure.data.json :only (read-json)])
  (:require [clojure.java.io :as io]
            [supplier-data.suppliers.eon.acquisition-filter :as f]))

(defn- load-switch-instruction
  [file]
  (-> file (io/resource) (io/reader) (read-json)))


(def elec-retention-switch {:instruction (load-switch-instruction "supplier_switches/eon/elec-retention.json")})

(deftest selects-only-retention-switches
  (is (= "Foo" "Bar")))